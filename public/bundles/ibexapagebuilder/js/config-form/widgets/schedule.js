(function (global, doc, ibexa) {
    const { BlockConfigScheduleFieldBase } = ibexa;

    const scheduleField = new BlockConfigScheduleFieldBase();

    scheduleField.init();
})(window, window.document, window.ibexa);
