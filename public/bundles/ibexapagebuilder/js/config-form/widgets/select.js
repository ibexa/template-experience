(function(doc, ibexa) {
    const { BlockConfigSelectFieldBase } = ibexa;

    const contentListSelect = new BlockConfigSelectFieldBase();

    contentListSelect.init();
})(window.document, window.ibexa);
